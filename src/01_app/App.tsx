import { Header } from "@widgets/Header"
import { BrowserRouter } from "react-router-dom"

function App() {
  return (
    <BrowserRouter>
      <Header theme='light'/>
    </BrowserRouter>
  )
}

export default App
